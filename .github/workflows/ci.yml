name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend-verify:
    name: Backend Verify
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      - name: Compile
        id: backend_compile
        continue-on-error: true
        run: |
          set -o pipefail
          mvn -B -ntp -DskipTests compile | tee backend-compile.log

      # NOTE: 当前 scripts/check_sql_conventions.sh 的 column-COMMENT 规则
      # 是按 MySQL 内联 COMMENT '...' 语法写的，对 PostgreSQL 方言
      # （migration-pg/）会全列误报。在脚本支持方言区分前，先把这一步降级为
      # 建议性检查：仍然运行并把结果归档到 backend-sql-check.log，但失败不再
      # 阻断流水线。修复脚本后再把 Assert 步骤里的 sql 判定打开即可。
      - name: SQL Convention Check (advisory)
        id: backend_sql
        if: ${{ steps.backend_compile.outcome == 'success' }}
        continue-on-error: true
        run: |
          set -o pipefail
          bash scripts/check_sql_conventions.sh | tee backend-sql-check.log

      - name: Unit Tests
        id: backend_tests
        if: ${{ steps.backend_compile.outcome == 'success' }}
        continue-on-error: true
        run: |
          set -o pipefail
          mvn -B -ntp test -Dsurefire.printSummary=true -DtrimStackTrace=false | tee backend-unit-tests.log

      - name: Backend Stage Summary
        if: always()
        run: |
          echo "compile outcome = ${{ steps.backend_compile.outcome }}"
          echo "sql outcome     = ${{ steps.backend_sql.outcome }}"
          echo "tests outcome   = ${{ steps.backend_tests.outcome }}"

      - name: Debug Report Paths
        if: always()
        run: |
          echo "Searching report directories under workspace..."
          find . -type d \( -name surefire-reports -o -name failsafe-reports -o -name "*-reports" \) -print || true

      - name: Upload Backend Test Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-test-reports-${{ github.run_number }}
          path: |
            backend-compile.log
            backend-sql-check.log
            backend-unit-tests.log
            **/target/surefire-reports/**
            **/target/failsafe-reports/**
            **/target/*-reports/**
          retention-days: 7
          if-no-files-found: ignore

      - name: Assert Backend Verify Passed
        if: always()
        run: |
          compile="${{ steps.backend_compile.outcome }}"
          sql="${{ steps.backend_sql.outcome }}"
          tests="${{ steps.backend_tests.outcome }}"

          if [[ "$compile" != "success" ]]; then
            echo "::error::Backend verify failed in Compile stage (outcome=$compile)"
            exit 1
          fi

          # SQL Convention Check 暂为建议性检查：失败仅作 warning，不阻断 CI。
          # 详见上文 SQL Convention Check (advisory) 步骤的注释。
          if [[ "$sql" != "success" ]]; then
            echo "::warning::SQL Convention Check stage reported issues (outcome=$sql); see backend-sql-check.log artifact"
          fi

          if [[ "$tests" != "success" ]]; then
            echo "::error::Backend verify failed in Unit Tests stage (outcome=$tests)"
            exit 1
          fi

  frontend-build:
    name: Frontend Build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: fronted/vue3-merged
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: fronted/vue3-merged/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Build Frontend
        run: npm run build

      - name: Upload Frontend Dist
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist-${{ github.run_number }}
          path: fronted/vue3-merged/dist/**
          retention-days: 3
