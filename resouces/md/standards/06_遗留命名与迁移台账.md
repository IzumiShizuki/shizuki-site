# 遗留命名与迁移台账

## 1. 顶层目录

| 遗留名 | 规范语义 | 范围 | 当前策略 |
| --- | --- | --- | --- |
| `fronted` | frontend root | 仓库顶层目录 | 冻结为 legacy path，本轮不重命名 |
| `resouces` | resources root | 仓库顶层目录 | 冻结为 legacy path，本轮不重命名 |

## 2. 前端状态与字段

| 遗留名 | 规范名 | 范围 | 当前策略 |
| --- | --- | --- | --- |
| `ball_slots` | `rail_slots` | 轻应用偏好结构 | 只保留兼容读写 |
| `ballSlots` | `rail_slots` | 前端历史对象 | 只保留兼容读取 |
| `floatingCodes` | `enabled_codes` + `rail_slots` | 前端历史对象 | 迁移时归一化 |
| `panelMode` | `panelKind` | 悬浮球运行时状态 | 本轮收口 |
| `panelCollectionId` | `activeCollectionId` | 悬浮球运行时状态 | 本轮收口 |

## 3. 前端术语与类名

| 遗留名 | 规范名 | 范围 | 当前策略 |
| --- | --- | --- | --- |
| `picker-panel` | `dock-panel` | 悬浮球展开面板 | 本轮收口 |
| `submenu-panel` | `dock-panel` | 悬浮球展开面板 | 本轮收口 |
| `palette-*` | `dock-*` | AppsPage 内部来源标识 | 本轮收口 |

## 4. 组件层

| 遗留名 | 规范名 | 范围 | 当前策略 |
| --- | --- | --- | --- |
| `AppMenuBar` 纯包装层 | `TopMenu` 主实现 | 全局菜单 | 本轮已删除包装层文件，后续不再新增同类别名 |
| 分散的 `top-toolbar` 模板 | `LightAppTopToolbar` | 轻应用窗体 | 本轮抽公共壳 |
| 分散的 `panel-collapse` 模板 | `CollapsiblePanel` | 轻应用窗体 | 本轮抽公共壳 |

## 5. 迁移顺序

1. 先统一文档与 registry
2. 再统一高频模板与运行时变量
3. 再清理纯包装组件与内部别名
4. Java / API 契约类遗留命名迁移改由 `10_后端遗留命名与迁移台账.md` 维护
