# 服务器部署交付总览（2026-03-02）

## 1. 基本信息
- 服务器 IP：`111.228.35.186`
- 主机名：`lavm-5gzieqa9yd`
- 系统：`Ubuntu 24.04.2 LTS`
- 内核：`6.8.0-53-generic`
- 文档更新时间：`2026-03-02 11:30 (Asia/Shanghai)`

## 2. 已部署服务清单（当前运行中）
| 服务 | 运行方式 | 镜像/进程 | 当前状态 | 主要端口 |
|---|---|---|---|---|
| New-API | Docker Compose | `calciumion/new-api:latest` | Up | `3000` |
| AstrBot | Docker Compose + systemd | `soulter/astrbot:latest` | Up | `6185`, `6199` |
| SillyTavern | 源码 + systemd | `node` | Up | `8000` |
| MCP Router Proxy | Docker | `mcprouter-custom:latest` | Up | 内部 `8025/8027` |
| Caddy 网关 | Docker | `caddy:2.8.4-alpine` | Up | `80`, `443` |
| MySQL | Docker | `mysql:8.4` | Up (healthy) | `3306` |
| Redis | Docker | `redis:7.4` | Up (healthy) | `6379` |
| Kafka | Docker | `apache/kafka:3.7.1` | Up (healthy) | `9092` |
| Nacos | Docker | `nacos/nacos-server:v2.3.2` | Up (healthy) | `8848`, `9848`, `9849` |

### 2.1 自启动状态（systemd）
- `docker`: `enabled/active`
- `astrbot-compose.service`: `enabled/active`
- `sillytavern-compose.service`: `enabled/active`

## 3. 访问入口（IP 与域名）
### 3.1 IP 入口（可直接访问）
- New-API：`http://111.228.35.186:3000`
- AstrBot：`http://111.228.35.186:6185`
- SillyTavern：`http://111.228.35.186:8000`（BasicAuth）
- MCP Router（网关）：`http://111.228.35.186/sse/<key>` 或 `/mcp/<key>`（需 Bearer）

### 3.2 域名入口（Caddy 已配置）
- `https://shizuki.online/new-api/*` -> `:3000`
- `https://site.shizuki.online` -> `:5173`
- `https://api.shizuki.online` -> `:3000`
- `https://bot.shizuki.online` -> `:6185`
- `https://tavern.shizuki.online` -> `:8000`
- `https://mcp.shizuki.online` -> `mcprouter-proxy:8025`（Bearer 校验）

说明：
- 域名路由配置在 `/opt/mcprouter/config/Caddyfile`。
- `site.shizuki.online` 仅反代，`5173` 的业务进程需你自行保持在线。

## 4. 端口与用途（用于京东云安全组备注）
| 端口 | 用途 | 建议开放范围 |
|---|---|---|
| 22/tcp | SSH 管理 | 建议限制到你的固定公网 IP |
| 80/tcp | Caddy HTTP / ACME 验证 | 公网 |
| 443/tcp | Caddy HTTPS | 公网 |
| 2376/tcp | Docker Remote API (TLS) | 强烈建议仅白名单 |
| 3000/tcp | New-API 原始端口 | 建议仅白名单或仅走 443 |
| 6185/tcp | AstrBot WebUI/API | 建议仅白名单或仅走 443 |
| 6199/tcp | AstrBot WS | 按需开放 |
| 8000/tcp | SillyTavern 原始端口 | 建议仅白名单或仅走 443 |
| 3306/tcp | MySQL | 强烈建议仅白名单/内网 |
| 6379/tcp | Redis | 强烈建议仅白名单/内网 |
| 9092/tcp | Kafka | 强烈建议仅白名单/内网 |
| 8848/tcp | Nacos API/UI | 强烈建议仅白名单/内网 |
| 9848/tcp | Nacos gRPC | 强烈建议仅白名单/内网 |
| 9849/tcp | Nacos gRPC | 强烈建议仅白名单/内网 |

## 5. 凭据与密钥（当前值）
> 这些内容属于敏感信息，请不要提交到公共仓库。建议你在确认业务可用后立即轮换。

### 5.1 服务器登录
- SSH：`root@111.228.35.186`
- 当前密码：`Izumi2486`

### 5.2 数据库与缓存（`/opt/infra/.env`）
- `MYSQL_ROOT_PASSWORD=zerXtj8Zl5lHA5HjgGor3Zln+6JCepEa`
- `MYSQL_APP_PASSWORD=1Fr7LM9FI8chNCInqEOZCOM5EKk2Kd+N`
- `REDIS_PASSWORD=wM2cNwdQHH62PHSvMCcSZ+9tkuIX3OO8`

### 5.3 AstrBot（`/opt/astrbot/.env`）
- `DASHBOARD_USERNAME=astrbot_admin`
- `DASHBOARD_PASSWORD_MD5=ef8892d7a0b810fe74b322dacb4c2a29`

### 5.4 SillyTavern（`/opt/sillytavern/app/config.yaml`）
- `basicAuthUser.username=izumi`
- `basicAuthUser.password=EoVlFRYNJkGWojutq2oz1hUQ`

### 5.5 Docker TLS
- 服务端证书目录：`/etc/docker/tls/`
- 已存在：`ca.pem`, `server-cert.pem`, `server-key.pem`, `cert.pem`, `key.pem`
- 远程 Docker 监听：`tcp://111.228.35.186:2376`（TLS 必须）

## 6. New-API 渠道分组与优先级（已按最新要求修正）
### 6.1 分组定义
- `default`：默认业务组
- `codex`：Codex 优先组
- `banana`：🍌 组
- `stable`：稳定组（按响应时间筛选）
- `sale`：特价全量组（全渠道）
- `sv1`：速率 v1（`response_time < 1000ms`）
- `sv2`：速率 v2（`response_time < 3000ms`）
- `vip`：云雾 + 柏拉图专组

### 6.2 当前优先级策略
- `Adekang Codex`：`1200`（最高）
- `🍌` 渠道：`1100`
- 其他常规渠道：`800`
- 云雾：`300`
- 柏拉图：`280`

主顺序：`codex > 🍌 > 其他 > 云雾 > 柏拉图`

### 6.3 当前统计
- `codex`: 3
- `banana`: 1
- `stable`: 12
- `sale`: 29
- `sv1`: 1
- `sv2`: 5
- `vip`: 2

### 6.4 新增 Codex 渠道
- 名称：`Adekang Codex`
- `base_url`: `https://openai.adekang.cc/v1`
- 已配置模型：`gpt-5-codex`, `gpt-5.1`, `gpt-5.1-codex-mini`, `gpt-5.2-codex`, `gpt-5.3-codex`, `gpt-5-codex-mini`, `gpt-5.1-codex`, `gpt-5.1-codex-max`, `gpt-5.2`

## 7. MCP Router（服务器侧）
### 7.1 当前已配置 MCP（`/opt/mcprouter/config/.env.toml`）
- `context7`
- `context7-mcp`
- `ddg-search`
- `mcp-deepwiki`
- `mcp-server-time`
- `sequential-thinking`

### 7.2 未在服务器侧启用（需本地保留或后续补迁）
- `beads`
- `desktop-commander`
- `playwright`
- `remotion-documentation`
- `mcp-shrimp-task-manager`

### 7.3 对外访问
- `https://mcp.shizuki.online/sse/<key>`
- `https://mcp.shizuki.online/mcp/<key>`

要求：
- 必须带 `Authorization: Bearer <MCP_GATEWAY_TOKEN>`

## 8. 防火墙状态（系统 UFW）
当前已放行（IPv4/IPv6）：
- `22`, `2376`, `3306`, `6379`, `9092`, `8848`, `9848`, `9849`, `6185`, `6199`, `3000`, `8000`, `80`, `443`

## 9. 常用运维命令
```bash
# 查看容器
ssh root@111.228.35.186 "docker ps"

# New-API 重启
ssh root@111.228.35.186 "cd /opt/new-api/deploy && docker compose -f docker-compose.yml restart new-api"

# AstrBot 重启
ssh root@111.228.35.186 "systemctl restart astrbot-compose.service"

# SillyTavern 重启
ssh root@111.228.35.186 "systemctl restart sillytavern-compose.service"

# Caddy 重启（域名反代）
ssh root@111.228.35.186 "cd /opt/mcprouter/docker && docker compose restart caddy"

# 看端口监听
ssh root@111.228.35.186 "ss -lntp"
```

## 10. 你接下来建议立刻做的事
1. 立即轮换已明文暴露凭据（SSH、MySQL、Redis、SillyTavern BasicAuth、第三方 API Key）。
2. 在京东云安全组把 `3306/6379/9092/8848/9848/9849/2376` 改为白名单源 IP。
3. 业务稳定后尽量只保留 `80/443/22` 公网，其他端口走内网或反代。
4. 若要长期公开 `mcp.shizuki.online`，建议额外加 IP 白名单与请求限流。

## 11. 变更记录（本次）
- New-API 新增 Adekang Codex 渠道并置为最高优先级。
- 按规则重排了渠道分组与优先级（`codex/banana/stable/sale/sv1/sv2/vip`）。
- MCP / 域名 / 端口 / 凭据信息已统一汇总到本文件。

## 12. 子域名 HTTPS 修复记录（2026-03-02）
### 12.1 现象与根因
- 现象：`api/bot/tavern/site/mcp.shizuki.online` 出现 TLS 握手失败，无法进入。
- 根因：历史首次签发时子域 DNS 尚未生效，ACME 校验报 `NXDOMAIN`，随后触发 `rateLimited`，导致子域证书未落盘。

### 12.2 本次执行动作
1. 核验 `80/443` 监听与 UFW 放行状态正常。
2. 清理 Caddy ACME 残留锁文件：`/opt/mcprouter/data/caddy_data/caddy/locks/issue_cert_*.lock`。
3. 重启 `mcprouter-caddy` 触发重新签发。
4. 观察日志确认成功：
   - `certificate obtained successfully` for:
   - `api.shizuki.online`
   - `bot.shizuki.online`
   - `tavern.shizuki.online`
   - `site.shizuki.online`
   - `mcp.shizuki.online`

### 12.3 验收结果
| 地址 | 结果 | 说明 |
|---|---|---|
| `https://api.shizuki.online` | `HTTP/2 200` | TLS 正常，路由到 New-API 正常 |
| `https://bot.shizuki.online` | `HTTP/2 200` | TLS 正常，路由到 AstrBot 正常 |
| `https://tavern.shizuki.online` | `HTTP/2 401` | TLS 正常，BasicAuth 生效（预期） |
| `https://mcp.shizuki.online/sse/context7` | `HTTP/2 401` | TLS 正常，Bearer 鉴权生效（预期） |
| `https://site.shizuki.online` | `HTTP/2 502` | TLS 正常；后端 `5173` 当前未启动（预期） |

### 12.4 子域上线检查清单（后续复用）
1. DNS：子域 A 记录均指向 `111.228.35.186`。
2. 防火墙：云安全组和 UFW 已放行 `80/443`。
3. Caddy：`mcprouter-caddy` 容器运行正常。
4. 证书：日志中出现 `certificate obtained successfully`。
5. 后端监听：`3000/6185/8000/8025(容器内)` 与 `5173(如启用 site)`。
