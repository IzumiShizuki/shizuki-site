# 服务器部署与交付总览 (v1.0)

## 1. 基础环境信息 (Infrastructure Metadata)

| 参数项 | 配置信息 |
| :--- | :--- |
| **服务器公网 IP** | `111.228.35.186` |
| **操作系统** | `Ubuntu 24.04.2 LTS` |
| **内核版本** | `6.8.0-53-generic` |
| **主机标识** | `lavm-5gzieqa9yd` |
| **更新时间** | `2026-03-02 11:30 (CST)` |

---

## 2. 在线服务拓扑 (Service Inventory)

| 服务名称 | 部署模式 | 当前状态 | 通讯端口 | 备注 |
| :--- | :--- | :--- | :--- | :--- |
| **New-API** | Docker Compose | 运行中 | `3000` | 模型聚合网关 |
| **AstrBot** | Compose + systemd | 运行中 | `6185` | 机器人控制后端 |
| **SillyTavern** | Source + systemd | 运行中 | `8000` | 角色扮演前端 |
| **Caddy Gateway** | Docker | 运行中 | `80/443` | 统一反向代理与 TLS 托管 |
| **Infra Stack** | Docker | 运行中 | 多端口 | MySQL, Redis, Kafka, Nacos |

---

## 3. 网络入口与路由 (Gateways & Domains)

### 3.1 域名解析映射
所有流量均由 Caddy 进行 TLS 终结并转发：
- `api.shizuki.online` → New-API 聚合服务
- `bot.shizuki.online` → AstrBot 管理后台
- `tavern.shizuki.online` → SillyTavern 交互界面
- `mcp.shizuki.online` → MCP Router 代理（需 Bearer 认证）
- `site.shizuki.online` → 站点前端（转发至 `:5173`）

### 3.2 端口准入策略 (Firewall)
| 协议/端口 | 职能定义 | 建议准入规则 |
| :--- | :--- | :--- |
| **22/tcp** | SSH 远程管理 | 仅限可信运维 IP |
| **80, 443/tcp** | HTTP/HTTPS | 全球公开 |
| **2376/tcp** | Docker TLS API | 严禁公开，仅限内网/白名单 |
| **3306, 6379/tcp** | DB/Cache | 严禁公开，仅限生产子网访问 |

---

## 4. 凭据管理 (Credentials)

> [!CAUTION]
> **安全警示：** 以下凭据处于初始交付状态。为保障生产安全，请在完成初次连接验证后立即启动轮换流程（Credential Rotation），严禁将带明文密文的文件提交至任何公共版本控制系统。

### 4.1 管理账户
- **SSH 账户:** `root@111.228.35.186` / `Izumi2486`
- **MySQL Root:** `zerXtj8Zl5lHA5HjgGor3Zln+6JCepEa`
- **Redis Auth:** `wM2cNwdQHH62PHSvMCcSZ+9tkuIX3OO8`

### 4.2 业务凭据
- **SillyTavern:** `izumi` / `EoVlFRYNJkGWojutq2oz1hUQ`
- **AstrBot:** `astrbot_admin` / `ef8892d7...` (MD5)

---

## 5. 运维手册 (Maintenance Guide)

### 常用运维操作汇总
```bash
# 实时日志观测
docker logs -f --tail 100 <container_name>

# 整体基础架构重启
cd /opt/infra && docker compose restart

# 触发 Caddy 配置重载
docker exec -it mcprouter-caddy caddy reload --config /etc/caddy/Caddyfile
```

---

## 6. 变更记录与修复日志 (Changelog)

### 2026-03-02 增量更新
- **渠道优化：** New-API 接入 `Adekang Codex` 渠道，权重上调至 1200（最高优先级）。
- **证书修复：** 处理了子域名首次签发时因 DNS 传播延迟导致的 ACME 锁死问题；目前所有子域名 HTTPS 均已通过 TLS 握手测试。
- **配置统合：** 迁移 MCP Router 配置至 `/opt/mcprouter`，统一环境变量管理。

> [!TIP]
> **后续行动建议：** 1. 完善京东云（JD Cloud）安全组白名单；2. 启用 Docker 远程 API 的 TLS 双向认证；3. 为 `site.shizuki.online` 部署持续集成（CI）脚本。
额外加 IP 白名单与请求限流。

## 11. 变更记录（本次）
- New-API 新增 Adekang Codex 渠道并置为最高优先级。
- 按规则重排了渠道分组与优先级（`codex/banana/stable/sale/sv1/sv2/vip`）。
- MCP / 域名 / 端口 / 凭据信息已统一汇总到本文件。

## 12. 子域名 HTTPS 修复记录（2026-03-02）
### 12.1 现象与根因
- 现象：`api/bot/tavern/site/mcp.shizuki.online` 出现 TLS 握手失败，无法进入。
- 根因：历史首次签发时子域 DNS 尚未生效，ACME 校验报 `NXDOMAIN`，随后触发 `rateLimited`，导致子域证书未落盘。

### 12.2 本次执行动作
1. 核验 `80/443` 监听与 UFW 放行状态正常。
2. 清理 Caddy ACME 残留锁文件：`/opt/mcprouter/data/caddy_data/caddy/locks/issue_cert_*.lock`。
3. 重启 `mcprouter-caddy` 触发重新签发。
4. 观察日志确认成功：
   - `certificate obtained successfully` for:
   - `api.shizuki.online`
   - `bot.shizuki.online`
   - `tavern.shizuki.online`
   - `site.shizuki.online`
   - `mcp.shizuki.online`

### 12.3 验收结果
| 地址 | 结果 | 说明 |
|---|---|---|
| `https://api.shizuki.online` | `HTTP/2 200` | TLS 正常，路由到 New-API 正常 |
| `https://bot.shizuki.online` | `HTTP/2 200` | TLS 正常，路由到 AstrBot 正常 |
| `https://tavern.shizuki.online` | `HTTP/2 401` | TLS 正常，BasicAuth 生效（预期） |
| `https://mcp.shizuki.online/sse/context7` | `HTTP/2 401` | TLS 正常，Bearer 鉴权生效（预期） |
| `https://site.shizuki.online` | `HTTP/2 502` | TLS 正常；后端 `5173` 当前未启动（预期） |

### 12.4 子域上线检查清单（后续复用）
1. DNS：子域 A 记录均指向 `111.228.35.186`。
2. 防火墙：云安全组和 UFW 已放行 `80/443`。
3. Caddy：`mcprouter-caddy` 容器运行正常。
4. 证书：日志中出现 `certificate obtained successfully`。
5. 后端监听：`3000/6185/8000/8025(容器内)` 与 `5173(如启用 site)`。
