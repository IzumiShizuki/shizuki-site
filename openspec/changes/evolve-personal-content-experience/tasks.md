## 1. 建立安全门槛与公共契约

- [x] 1.1 为 `PermissionAspect` 补充失败测试，覆盖控制器类级 `@RequireGroup`、方法级 `@RequirePermission`、跨维度同时存在时的 AND 语义以及同维度方法声明覆盖类声明。
- [x] 1.2 修正权限切面匹配与规则合并逻辑，使类级和方法级注解均在进入控制器前生效、跨维度约束合取且仅同维度允许方法覆盖，并保持既有受保护接口行为不回退。
- [x] 1.3 增加鉴权集成测试，分别验证匿名请求返回 401、已登录但无权限返回 403、具备权限返回 200，且响应不泄露受保护资源是否存在。
- [x] 1.4 在 MySQL 与 PostgreSQL 配对迁移中登记 `life.content.manage`、`site.widgets.manage`、`media.derivative.manage` 权限并关联管理员组，不写入虚构内容数据。
- [x] 1.5 定义生命周期、可见性、派生类型、派生状态、新鲜度、下载策略与统一错误码枚举，并为 JSON 兼容性增加序列化测试。
- [x] 1.6 定义公共游标分页、管理端乐观版本、媒体处理状态和访客错误响应 DTO，并记录游标稳定排序及无存储键暴露的契约测试。
- [x] 1.7 增加个人内容、外部组件和媒体派生的独立功能开关与配置校验，使各域可单独发布或回滚且默认不会产生上游请求。
- [x] 1.8 实现后台审计记录器，保存操作者、动作、目标、结果与时间，并用成功/失败测试证明其会清除凭据、签名媒体地址和访客原始坐标。

## 2. 建立个人内容领域与数据库基础

- [x] 2.1 创建 `V431` 与 `V1011` 配对迁移，新增 `CTN_PHOTO`、`CTN_ALBUM`、`CTN_ALBUM_PHOTO`、`CTN_MOMENT`、`CTN_MOMENT_PHOTO` 表及必要外键、唯一约束、查询索引、safe-restore 状态与 `purge_after`。
- [x] 2.2 在同一对迁移中新增 `CTN_SITE_LOCATION`、单例 `CTN_SITE_WIDGET_CONFIG`、`CTN_QUOTE`、`CTN_DAILY_QUOTE_SNAPSHOT`、`CTN_WEATHER_SNAPSHOT` 与 `MDA_ASSET_VARIANT`，明确时区、精度、状态、唯一默认地点和引用约束。
- [x] 2.3 为迁移添加空库与既有数据升级验证，确认两个数据库方言结构等价、`quote_date`/variant scope/association media ref 唯一约束有效、可重复启动且不生成示例相册、动态或照片。
- [x] 2.4 实现相册、照片、动态、地点、语录、快照与媒体派生的实体和 Mapper，并用持久化测试覆盖字段映射、排序、可查询的 `RECYCLED` 与仅永久清理后使用的逻辑删除 tombstone。
- [x] 2.5 实现 `DRAFT`、`PUBLISHED`、`ARCHIVED`、`RECYCLED` 生命周期服务和 `PUBLIC`、`UNLISTED`、`PRIVATE` 可见性矩阵，回收时记录 `DRAFT/ARCHIVED` 安全恢复目标与保留期，恢复永不自动重新发布，并拒绝非法状态跃迁。
- [x] 2.6 为生命周期与可见性矩阵编写表驱动测试，覆盖公共列表、直达详情、管理预览、搜索/站点地图资格与媒体资格。
- [x] 2.7 为公开内容生成高熵不可枚举标识，并以发布时间、排序键和标识建立稳定游标，验证并发新增时不重复或漏读已有页。
- [x] 2.8 为会发生重排或覆盖的管理命令加入版本字段与 ETag 校验，冲突时返回当前版本而不是静默覆盖。

## 3. 构建隐私优先的照片派生管线

- [x] 3.1 扩展 `ObjectStorageClient`，支持阿里云 OSS 图片处理、`sys/saveas` 保存派生对象和私有对象签名访问，并为请求参数编码增加单元测试。
- [x] 3.2 实现上传草稿校验：按魔数和安全解码确认格式，限制字节、像素与帧数，计算 SHA-256，并将 EXIF 仅保留在受保护的处理上下文。
- [x] 3.3 对伪装 MIME、损坏图片、超大像素、超限动态图片和重复上传建立测试夹具，确保失败对象不会进入可发布状态。
- [x] 3.4 实现持久化派生任务与 worker，以 `(source, variant, delivery_scope, processor_version)` 幂等领取，并支持有限重试、故障原因、版本化对象键和进程重启后的继续处理。
- [x] 3.5 生成必需的 `THUMB_WEBP`、`DISPLAY_WEBP` 与 `FULL_SANITIZED`，并把 AVIF 作为经 OSS 能力预检后才启用的可选派生，而非发布前置条件。
- [x] 3.6 对每个访客派生执行处理后元数据复检；只有源归属、完整解码、安全限制与 EXIF 验证均通过才进入可信 `APPROVED/READY`，并用带 GPS、相机和设备信息的样本证明所有访客派生均安全。
- [x] 3.7 建立派生对象 `delivery_scope` 与内容关联授权，公共响应仅返回站内 opaque 媒体路由，绝不返回 bucket、object key、OSS 签名或长期原图 URL。
- [x] 3.8 实现站内媒体 gateway：由高熵 `mediaRef` 或短时 capability 定位具体 album-photo/moment-photo 关联，逐次校验内容状态、variant 与关联级下载策略，在服务端签名并流式/内部反代 OSS 且禁止重定向；验证 JSON、响应头、浏览器请求、缓存和日志均不泄露上游地址。
- [x] 3.9 实现公开变私有、归档和回收时的引用感知撤权与 CDN 失效；共享照片只有在仍被其他合格公开内容引用时才继续公开交付。
- [x] 3.10 实现 `NONE`、`SANITIZED`、`ORIGINAL` 下载策略；启用原图下载前要求明确隐私确认，并始终通过短时鉴权链路交付原图。
- [x] 3.11 实现照片与派生的回收、恢复、保留期校验、永久清理预检和 dry-run，保证到期前或任何非已清理内容仍引用的原图/派生都不会被删除，首期不启用自动清理任务。

## 4. 实现相册与生活动态后端

- [x] 4.1 实现相册草稿创建、编辑、封面选择、发布、归档、回收、恢复与永久清理服务，并在发布前校验封面及必需派生均就绪。
- [x] 4.2 实现相册照片批量复用、附加、移除和原子重排命令，使用 ETag 防止两个后台会话互相覆盖顺序。
- [x] 4.3 实现相册管理 API，包含独立草稿/私有预览、处理状态、失败重试、可见性、下载策略与审计信息，并确保预览不经过公共端点或公共缓存且全部写操作受新权限保护。
- [x] 4.4 实现公开相册游标列表、精选预览和详情 API，确保 `UNLISTED` 仅可不可枚举直达并返回 `private, no-store`、`PRIVATE` 不泄露存在性、只有合格 `PUBLIC` 内容进入搜索/Feed/站点地图且空库返回真实空结果。
- [x] 4.5 实现动态草稿创建、正文编辑、照片复用与重排、发布、归档、回收、恢复和清理服务，并对照片未就绪阻止发布。
- [x] 4.6 实现动态管理 API 和公共游标 feed/detail API，应用与相册一致的生命周期、可见性、媒体授权和 `noindex` 契约。
- [x] 4.7 为相册和动态建立按标识/列表的缓存与定向失效，在发布、撤回、重排和媒体状态变化后避免返回旧可见性或旧顺序。
- [x] 4.8 添加服务与控制器测试，覆盖共享照片、关联级原图授权、并发重排、待处理派生、GPS 未审核时无地点、公开转私有、未列出直达、私有/不存在同形 404、归档撤回、回收安全恢复及无内容状态。

## 5. 实现天气与今日一言数据源

- [x] 5.1 定义天气和语录 provider 接口、归一化模型及固定 HTTPS 主机白名单，并统一限制连接/读取超时、响应大小和 JSON schema。
- [x] 5.2 实现 Open-Meteo provider 与单例站点组件配置服务，保存经管理员校验的地点、时区、最大 stale 年龄和来源模式，并以版本化事务切换唯一默认地点及定向失效旧缓存。
- [x] 5.3 实现站点天气快照缓存、数据库 last-good、最大 stale 判定与 Redis 分布式刷新锁，返回观测时间、抓取时间、`fresh/stale/unavailable` 和来源归属；Redis/锁不可用时只读数据库事实并暂停上游刷新。
- [x] 5.4 实现访客附近天气 POST 接口，只接受明确同意后提交的合法坐标，按配置精度匿名归一化，以每调用方/全局 token bucket 和有界短 TTL cache 控制滥用，不持久化原始坐标或身份关联，并返回 `private, no-store`。
- [x] 5.5 增加天气 provider 成功、超时、非法响应、Redis 故障、并发击穿、超过 max-stale、附近限流/键上限、无快照和越界坐标测试，验证失败时不伪造数据、不绕过限制且附近数据不进入持久库/共享缓存。
- [x] 5.6 实现 Hitokoto provider 与每日快照计划任务，以站点时区和分布式锁为当天至多提交一个不可变 canonical snapshot，并保留来源、类型和抓取时间。
- [x] 5.7 实现本地语录 CRUD、审核状态与持久化“Hitokoto / 本地精选”来源切换；上游结果不会自动进入已审核库，切到本地后计划任务与缺失回填均停止联系上游并只从已审核语录生成快照。
- [x] 5.8 实现今日一言 last-good 回退、不可用状态、仅补齐缺失当天快照的手动刷新命令和只遍历已审核本地语录的“换一句”，保证访客动作不联系 Hitokoto，且任何普通刷新都不会覆盖当天已成功快照。
- [x] 5.9 实现访客天气/今日一言读取 API 和管理员地点/来源/状态 API，给访客路径配置明确匿名权限并给管理写操作配置新权限。
- [x] 5.10 增加天气归属、语录来源、缓存新鲜度、时区跨日、同日快照不可变、来源原子切换/定向失效、Redis/上游故障和多实例并发任务的集成测试。

## 6. 建立全站动效与液态材质基础

- [x] 6.1 实现单一 `useMotionPreference` 状态源，持久化 `immersive/soothing`，默认沉浸，并把 `vivid` 映射到沉浸、`calm/off` 映射到舒缓。
- [x] 6.2 为首次访问、损坏旧值、路由切换、会话恢复和旧值迁移编写前端测试，确保界面只出现“沉浸/舒缓”两档。
- [x] 6.3 将操作系统 `prefers-reduced-motion` 仅映射为临时有效舒缓模式，保留保存值；测试设备、帧率、电池、网络和视口不会触发自动降档。
- [x] 6.4 在 App、Home、About、Blog、Albums、Moments、灯箱与抽屉接入统一有效模式，移除页面局部三档开关和互相覆盖的动效状态。
- [x] 6.5 建立语义化 `LiquidSurface`、共享 SVG 滤镜定义和 CSS 能力回退，按昼夜主题提供液态折射或基础半透明玻璃而不复制滤镜 ID。
- [x] 6.6 明确每个交互只由一个动画拥有者控制 transform，并为菜单、卡片与视图过渡增加无双重位移、无退出闪烁的回归测试。
- [x] 6.7 为键盘焦点、文本对比度、深色材质、浏览器无滤镜能力与 Focus 状态增加组件级视觉/可访问性测试。

## 7. 重构公共滚动外壳与导航

- [x] 7.1 将普通公共路由统一到 App 管理的唯一纵向滚动根，保留博客编辑器的固定工作区例外，并测试导航只监听当前有效滚动源。
- [x] 7.2 实现 `ThreeColumnContentShell`、`StickyCardStack`、`HorizontalCardRail` 与 `AuxiliaryDrawer`，采用桌面 `>=1200` 三栏、平板 `900–1199` 两栏、移动 `<900` 单栏。
- [x] 7.3 实现侧栏 sticky 边界、中心流最小宽度、横向轨道滚轮仅在可继续横滚时消费事件，以及轨道边缘把滚动归还页面的测试。
- [x] 7.4 重构 TopMenu 的顶部完整态与越阈值紧凑液态态，保留 Home、Blog、Music、Apps、AI Hub 五个一级入口和现有路由高亮。
- [x] 7.5 实现 Site 菜单的 About、Albums、Moments 入口及真实最近内容预览；无合格内容时显示设计空态而不借用头像、壁纸或夹具伪造。
- [x] 7.6 为 Site 菜单实现点击外部、Escape、焦点返回、路由切换和打开关闭不重置播放器/组件状态的行为测试。
- [x] 7.7 实现移动端紧凑顶部入口与生活抽屉，保证触控目标、滚动锁定、焦点圈定和关闭后滚动位置恢复。
- [x] 7.8 验证 Focus 激活时完整/紧凑/Site/移动导航均隐藏且不可聚焦，退出时按当前路由和滚动位置恢复，而 Home 安静的 About 入口继续位于三枚岛之外。

## 8. 构建公共相册与动态体验

- [x] 8.1 建立相册、动态、天气与语录 API 客户端及 composable，统一处理加载、真实空态、错误、竞态取消、游标合并和签名媒体刷新。
- [x] 8.2 实现相册列表与独立详情路由，在沉浸模式提供 Polaroid 层叠与 Memory Lens 连续过渡，在舒缓模式提供稳定卡片和轻淡入。
- [x] 8.3 实现可访问的深色媒体灯箱，支持前后导航、Escape、触控、键盘、标题/替代文本、关闭后焦点返回，且永不切换到未授权原图。
- [x] 8.4 实现动态时间流、照片组和页内展开详情，使展开/收起不重置滚动、天气、今日一言或全局播放器。
- [x] 8.5 为公开与未列出内容实现复制分享链接回退，并保证未列出页面设置 `noindex`、分享内容不包含签名 URL 或私有存储信息。
- [x] 8.6 接入新路由标签、路由 view key 与浏览器不支持 View Transitions 时的 CSS 回退，验证前进/后退保持正确选中和焦点。
- [x] 8.7 增加公开页面组件测试，覆盖沉浸/舒缓、昼/夜、空数据、上游失败、媒体失败、未列出直达、私有拒绝和无虚构占位内容。
- [x] 8.8 实现共享 WeatherCard 与 DailyQuoteCard，页内展示来源、观测/抓取时间、新鲜度和诚实空态；天气详情原位展开，“我的天气”只在明确点击后申请定位并发送 POST，可无扰回到站点天气，拒绝/未点击路径不调用 geolocation 或附近接口。

## 9. 迁移 About 与 Blog 到三栏内容系统

- [x] 9.1 在不改变现有作者数据契约的前提下拆分 `AuthorPage` 为页面编排、介绍中心流、卡片 rail 与辅助栏组件，并建立迁移前后的关键行为回归测试。
- [x] 9.2 将 About 组合为桌面三栏、平板两栏、移动单栏，接入相册/动态精选、天气与今日一言，确保各模块失败隔离且主要介绍仍可阅读。
- [x] 9.3 将 `BlogList` 迁移到同一三栏外壳，在文章列表中穿插受控的相册/动态预览，并保持既有筛选、分页、封面和列表滚动行为。
- [x] 9.4 将超大 `BlogPage` 拆为 reader 与 editor 工作区；reader 使用公共滚动根和辅助栏，editor 保留固定布局、预览与保存行为。
- [x] 9.5 完成 reader/editor 回归后移除列表兼容分支和重复滚动监听，验证文章深链、后退、草稿预览与编辑器未保存状态。
- [x] 9.6 为文章卡片到 reader 建立克制的连续空间过渡及舒缓/无能力回退，保证入口焦点、正文标题和返回滚动语义正确，且不把强折射 Lens 扩散到普通文章卡片。
- [x] 9.7 将推荐音乐卡片投影到既有全局播放器，仅调用既有打开/选曲能力，不自动播放、不重建实例、不重置当前队列或进度。
- [x] 9.8 验证天气、语录、相册和动态任一请求失败时 About/Blog 主内容、导航与播放器仍独立可用并显示局部诚实错误态。

## 10. 构建后台个人内容工作台

- [x] 10.1 将既有后台可观察地整理为 System / Content / Site Appearance / Feedback 分组，并在 Content/Site 下按需加载“相册 / 动态 / 今日一言 / 站点组件”；只有满足 ADMIN 基线及相应权限的用户显示入口，同时继续依赖服务端授权。
- [x] 10.2 实现相册创建编辑、批量上传/复用、逐项处理进度、失败重试、封面、拖拽重排、显式位置标签审核、可见性、下载策略、预览与发布流程。
- [x] 10.3 为相册重排和编辑实现 ETag 冲突界面，展示服务器当前版本并允许人工重新应用，而不是自动覆盖另一会话的修改。
- [x] 10.4 实现动态正文编辑、照片复用/重排、可见性、预览、发布和撤回流程，并复用相同媒体处理状态组件。
- [x] 10.5 实现语录新增、编辑、审核、本地精选开关、今日快照查看与手动刷新，清楚展示当前来源和 last-good 状态。
- [x] 10.6 实现站点地点/时区、天气刷新状态与推荐音乐配置界面，校验坐标并避免在 UI 或日志中暴露 provider 内部错误或访客位置。
- [x] 10.7 实现统一回收站、保留到期状态、安全恢复和永久清理确认；dry-run 获取绑定管理员/目标/版本/时效的单次 capability，服务端拒绝绕过 UI、重放、过期或改目标的清理请求。
- [x] 10.8 添加后台组件与端到端权限测试，覆盖入口隐藏、直接 URL 拒绝、批量部分失败、处理中阻止发布、版本冲突和清理引用保护。

## 11. 集成验证与分阶段发布

- [x] 11.1 运行前端目标 Vitest、全量测试、lint 与生产构建，修复由滚动根、路由拆分、动效状态和新页面引入的回归。（仓库无 lint 脚本；全量 Vitest 870/870 与生产构建通过。）
- [x] 11.2 使用项目 Java 17 激活脚本运行 monolith 模块单元/集成测试与构建，重点核对权限、迁移、缓存、定时任务和 OSS 客户端。（隔离宿主回环限制的 10 类后 reactor 全绿；默认套件的既有 JDK 回环失败单独记录；构建通过。）
- [ ] 11.3 在 PostgreSQL 目标环境验证配对迁移和回滚预案，并用 GPS JPEG、伪装 MIME、超大图、损坏图与动态图片完成 OSS 隐私管线验收。
- [ ] 11.4 以 `1440x900`、`1024x768`、`390x844` 检查三/二/一栏、横向轨道、导航收拢、灯箱和后台，并覆盖昼/夜与沉浸/舒缓组合。
- [ ] 11.5 用鼠标、触控和纯键盘完成菜单、卡片、抽屉、分享、灯箱与播放器流程；检查焦点顺序、焦点返回、对比度、替代文本和减少动态。
- [x] 11.6 模拟 Open-Meteo、Hitokoto、Redis 与 OSS 的成功、超时、非法响应和不可用，确认 last-good/空态诚实、无缓存击穿且主内容不被连带阻断。
- [x] 11.7 审查来源归属、SSRF 白名单、访客坐标日志、签名 URL、EXIF 与对象键，证明公共响应和日志中不存在隐私或存储信息泄露。
- [x] 11.8 编写按“权限修复 → 媒体/后台 → 只读公共 API → 公共 UI”推进的发布 runbook，记录每阶段监控、缓存撤权和单域回滚步骤；实际生产启用须另获明确授权。
- [x] 11.9 建立发布 gate 与可重复 smoke checklist，检查无虚构相册/动态/照片种子、真实空态、公开转私有和归档撤权；本地实施不执行生产开关、部署或推送。
- [ ] 11.10 对照 proposal、design 与八份 capability spec 完成最终验收，并运行 `openspec validate evolve-personal-content-experience --type change --strict --no-interactive`。
