## 1. Workshop Resource Classification

- [x] 1.1 Add deterministic importer coverage for rejecting a preview-only native Workshop project and selecting a real runtime media file over its preview.
- [x] 1.2 Exclude known presentation-only filenames from Workshop directory visual selection and report an actionable native-project fallback when no playable visual remains.

## 2. Verification

- [x] 2.1 Run focused wallpaper importer tests and the applicable module test suite.
- [x] 2.2 Validate the OpenSpec change strictly and inspect the final diff for scoped behavior and test coverage.
